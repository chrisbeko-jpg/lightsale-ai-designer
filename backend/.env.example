DATABASE_URL=postgresql+asyncpg://lightsale:lightsale@localhost:5432/lightsale
UPLOAD_DIR=uploads
CORS_ORIGINS=http://localhost:3000,https://web-topaz-mu-36.vercel.app
# Optional; defaults to https://.*\.vercel\.app when unset in code
# CORS_ORIGIN_REGEX=
ENABLE_POSTGIS=false

# Railway: attach a volume and set UPLOAD_DIR=/data/uploads for persistent floor plans.
