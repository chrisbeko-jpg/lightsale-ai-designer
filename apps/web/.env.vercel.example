# Vercel: Project → Settings → Environment Variables
# Apply to Production, Preview, and Development as needed.

NEXT_PUBLIC_API_URL=https://your-backend-host.example.com

# Until the FastAPI backend is deployed, leave unset to default to http://localhost:8000
# (browser calls from the Vercel URL will fail without a public API).
